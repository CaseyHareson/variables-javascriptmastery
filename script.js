// VARIABLE NAMING RULES:
// 1. The name should be unique.
// 2. The name should not be any reserved keyword.
// 3. Tha name must start with a character, _, $.

// Create a variable usig "var". 
// Avoid usig "var" in JavaScript!!!
//var _$variableName = "Test"; 

// Create a variable using "let".  
let _$variableName = "Welcome To Variables";

// Create a variable using "const".
const _$constantName = "This string is constant";

// Console display value of the variable. 
console.log(_$variableName);
console.log(_$constantName);

// Variable declared with "let" is modifyable in JavaScript.
_$variableName = "Hello!" ;

// Variables declared with "const" have global muted value. 
//_$constantName = "Opps, constants do not reassign value"; // This line will throw an error!!! 

// Console display value of the variable. 
console.log( _$variableName);
console.log(_$constantName);
